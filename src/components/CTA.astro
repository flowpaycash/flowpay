---
---

<section class="cta-section">
  <div class="cta-bg-glow"></div>
  <div class="cta-container">
    <div class="cta-glass">
      <h2 class="cta-title">O futuro do seu dinheiro <span class="gradient-text">começa aqui</span>.</h2>
      <p class="cta-subtitle">Junte-se a milhares de usuários que já escolheram a liberdade.</p>
      <a href="/registro" class="btn-premium primary">
        Criar conta grátis
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
      </a>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: 8rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-bg-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 60%;
    height: 60%;
    background: radial-gradient(circle, rgba(255, 0, 122, 0.12) 0%, transparent 70%);
    filter: blur(40px);
    z-index: 0;
    pointer-events: none;
  }

  .cta-container {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .cta-glass {
    background: rgba(15, 15, 15, 0.4);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 32px;
    padding: 4rem 3rem;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4), inset 0 1px 1px rgba(255, 255, 255, 0.1);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .cta-title {
    font-size: clamp(2rem, 6vw, 3rem);
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.03em;
    line-height: 1.1;
    text-shadow: 0 0 40px rgba(0, 0, 0, 0.5);
  }

  .gradient-text {
    background: linear-gradient(90deg, #ff007a 0%, #ff7a00 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cta-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.3rem);
    color: rgba(255, 255, 255, 0.7);
    max-width: 600px;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .btn-premium {
    padding: 20px 40px;
    border-radius: 20px;
    font-size: 1.1rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: inline-flex;
    align-items: center;
    gap: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    -webkit-tap-highlight-color: transparent;
  }

  .btn-premium.primary {
    background: linear-gradient(135deg, #ff007a, #ff7a00);
    color: white;
    box-shadow: 0 0 30px rgba(255, 0, 122, 0.3);
  }

  .btn-premium.primary:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 0 50px rgba(255, 0, 122, 0.5);
  }

  .btn-premium.primary:active {
    transform: scale(0.96);
    transition: all 0.1s ease;
  }

  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: 5rem 1.25rem;
    }

    .cta-glass {
      padding: 3rem 1.5rem;
      border-radius: 28px;
    }

    .btn-premium {
      width: 100%;
      justify-content: center;
      padding: 18px 32px;
    }
  }
</style>
