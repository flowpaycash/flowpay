---

---

<section id="features" class="home-process-section">
  <div class="section-bg-glow"></div>
  <div class="home-container">
    <h2 class="home-title">
      Você cria um link de pagamento e recebe direto na sua carteira digital.<br
      /><span class="gradient-text">Sem intermediários.</span>
    </h2>
    <p class="home-subtitle">
      Imagine sentir o poder total sobre seu dinheiro nas suas mãos. Agora você
      pode. FlowPay conecta seu dinheiro diretamente ao futuro das finanças, sem
      intermediários limitando suas escolhas. Enquanto todos falam de liberdade
      digital, você já está vivendo ela.
    </p>

    <div class="process-steps">
      <!-- Step 1 -->
      <div class="step-card" style="--delay: 0.6s">
        <div class="step-icon-wrapper">
          <div class="step-number">1</div>
          <div class="step-icon">
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <line x1="12" y1="1" x2="12" y2="23"></line>
              <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"
              ></path>
            </svg>
          </div>
        </div>
        <h3>Quem?</h3>
        <p>
          E-commerce;
          <br />
          Criadores de conteúdo;
          <br />
          Influencers;
          <br />
          Empresas;
          <br />
          Qualquer pessoa que queira receber pagamentos
        </p>
      </div>

      <!-- Connector -->
      <div class="step-connector">
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          class="pulse-anim"
        >
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </div>

      <!-- Step 2 -->
      <div class="step-card highlight" style="--delay: 0.8s">
        <div class="step-icon-wrapper">
          <div class="step-number">2</div>
          <div class="step-icon">
            <!-- Smart Factory / Chip Icon -->
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <rect x="4" y="4" width="16" height="16" rx="2" ry="2"></rect>
              <rect x="9" y="9" width="6" height="6"></rect>
              <line x1="9" y1="1" x2="9" y2="4"></line><line
                x1="15"
                y1="1"
                x2="15"
                y2="4"></line>
              <line x1="9" y1="20" x2="9" y2="23"></line><line
                x1="15"
                y1="20"
                x2="15"
                y2="23"></line>
              <line x1="20" y1="9" x2="23" y2="9"></line><line
                x1="20"
                y1="14"
                x2="23"
                y2="14"></line>
              <line x1="1" y1="9" x2="4" y2="9"></line><line
                x1="1"
                y1="14"
                x2="4"
                y2="14"></line>
            </svg>
          </div>
        </div>
        <h3 class="gradient-text">O que muda?</h3>
        <p>
          PIX → Cripto;
          <br />
          Venda direta, sem taxa;
          <br />
          Recebe sem banco;
          <br />
          Sem intermediários;
          <br />
          Sem espera;
          <br />
          Liquidez instantânea;
        </p>
      </div>

      <!-- Connector -->
      <div class="step-connector">
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          class="pulse-anim"
        >
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </div>

      <!-- Step 3 -->
      <div class="step-card" style="--delay: 1.0s">
        <div class="step-icon-wrapper">
          <div class="step-number">3</div>
          <div class="step-icon">
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
          </div>
        </div>
        <h3>Resultado?</h3>
        <p>
          Vende + rápido;
          <br />
          Recebe na hora;
          <br />
          Sem burocracia;
          <br />
          Sem espera;
          <br />
          Liquidez instantânea;
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .home-process-section {
    padding: 5rem 1.25rem;
    position: relative;
    z-index: 2;
    text-align: center;
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .home-process-section {
      padding: 8rem 2rem;
    }
  }

  .section-bg-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80vw;
    height: 80vw;
    background: radial-gradient(
      circle at center,
      rgba(255, 0, 122, 0.08) 0%,
      transparent 70%
    );
    z-index: 0;
    pointer-events: none;
  }

  .home-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .home-title {
    font-size: clamp(2rem, 6vw, 3rem);
    font-weight: 800;
    margin-bottom: 1.5rem;
    color: #fff;
    line-height: 1.1;
    letter-spacing: -0.03em;
    text-shadow: 0 0 40px rgba(0, 0, 0, 0.5);
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .gradient-text {
    background: linear-gradient(90deg, #ff007a 0%, #ff4da6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .home-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 3rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.6;
    animation: fadeInUp 0.8s ease-out 0.4s both;
  }

  @media (min-width: 768px) {
    .home-subtitle {
      margin-bottom: 5rem;
    }
  }

  .process-steps {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
    position: relative;
  }

  .step-card {
    flex: 1;
    background: rgba(15, 15, 15, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 28px;
    padding: 2rem 1.25rem;
    position: relative;
    transition:
      background 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      border-color 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      box-shadow 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    box-shadow:
      0 15px 40px rgba(0, 0, 0, 0.3),
      inset 0 1px 1px rgba(255, 255, 255, 0.05);
    -webkit-tap-highlight-color: transparent;
    animation: fadeInUp 0.8s ease-out var(--delay, 0.6s) both;
  }

  @media (min-width: 480px) {
    .step-card {
      padding: 2.5rem 1.5rem;
    }
  }

  @media (hover: hover) {
    .step-card:hover {
      transform: translateY(-8px);
      background: rgba(15, 15, 15, 0.5);
      border-color: rgba(255, 0, 122, 0.2);
      box-shadow:
        0 25px 50px rgba(0, 0, 0, 0.4),
        inset 0 1px 1px rgba(255, 255, 255, 0.1);
    }
  }

  .step-card:active {
    transform: scale(0.97);
    transition: transform 0.1s ease;
  }

  .step-card.highlight {
    background: rgba(255, 0, 122, 0.06);
    border-color: rgba(255, 0, 122, 0.2);
    box-shadow:
      0 15px 40px rgba(0, 0, 0, 0.3),
      0 0 30px rgba(255, 0, 122, 0.05),
      inset 0 1px 1px rgba(255, 255, 255, 0.08);
  }

  @media (hover: hover) {
    .step-card.highlight:hover {
      border-color: rgba(255, 0, 122, 0.4);
      box-shadow:
        0 25px 50px rgba(0, 0, 0, 0.4),
        0 0 40px rgba(255, 0, 122, 0.1),
        inset 0 1px 1px rgba(255, 255, 255, 0.1);
    }
  }

  .step-icon-wrapper {
    position: relative;
    display: inline-block;
    margin-bottom: 1.5rem;
  }

  .step-number {
    position: absolute;
    top: -10px;
    right: -10px;
    width: 24px;
    height: 24px;
    background: #ff007a;
    color: white;
    font-weight: 700;
    font-size: 0.8rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 10px rgba(255, 0, 122, 0.5);
    z-index: 2;
  }

  .step-icon {
    width: 80px;
    height: 80px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition:
      border-color 0.3s ease,
      box-shadow 0.3s ease;
  }

  @media (hover: hover) {
    .step-card:hover .step-icon {
      border-color: rgba(255, 255, 255, 0.2);
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
    }
  }

  .step-card.highlight .step-icon {
    color: #ff007a;
    background: rgba(255, 0, 122, 0.1);
    box-shadow: 0 0 20px rgba(255, 0, 122, 0.2);
  }

  .step-card h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #fff;
    letter-spacing: -0.02em;
  }

  .step-card p {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
  }

  .step-connector {
    display: flex;
    align-items: center;
    justify-content: center;
    padding-top: 3.5rem;
    color: rgba(255, 255, 255, 0.2);
    animation: fadeInUp 0.8s ease-out 0.7s both;
  }

  .pulse-anim {
    animation: pulse-right 1.5s infinite;
  }

  @keyframes pulse-right {
    0% {
      transform: translateX(0);
      opacity: 0.3;
    }
    50% {
      transform: translateX(5px);
      opacity: 1;
    }
    100% {
      transform: translateX(0);
      opacity: 0.3;
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 767px) {
    .process-steps {
      flex-direction: column;
      gap: 1.25rem;
    }

    .step-connector {
      transform: rotate(90deg);
      padding: 0;
      margin: -8px 0;
    }

    .step-card {
      width: 100%;
    }
  }
</style>
