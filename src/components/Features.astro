---

---

<section id="features" class="home-process-section">
  <div class="section-bg-glow"></div>
  <div class="home-container">
    <h2 class="home-title">
      Veja como é simples<br />
    </h2>
    <p class="home-subtitle">
      Nenhuma etapa técnica. Nenhum banco no meio. Você cria, compartilha e
      recebe, só isso. E ainda acompanha as transações em tempo real no seu
      dashboard.
    </p>

    <div class="process-steps">
      <!-- Step 1 -->
      <div class="step-card" style="--delay: 0.5s">
        <div class="step-icon-wrapper">
          <span class="step-number">1</span>
          <div class="step-icon">
            <svg
              width="28"
              height="28"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.75"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <line x1="12" y1="1" x2="12" y2="23"></line>
              <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"
              ></path>
            </svg>
          </div>
        </div>
        <div class="step-content">
          <h3>Cria o link</h3>
          <p>
            Define o valor, dá um nome ao pagamento e gera seu link em segundos
            — direto no Dashboard.
          </p>
        </div>
      </div>

      <!-- Connector -->
      <div class="step-connector" aria-hidden="true">
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2.5"
          class="pulse-anim"
        >
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </div>

      <!-- Step 2 -->
      <div class="step-card highlight" style="--delay: 0.7s">
        <div class="step-icon-wrapper">
          <span class="step-number">2</span>
          <div class="step-icon">
            <svg
              width="28"
              height="28"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.75"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path
                d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.67 3.46 2 2 0 0 1 3.64 1h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L7.91 8.6a16 16 0 0 0 6 6l.96-.97a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 21.5 16"
              ></path>
              <path d="M16 2a4 4 0 0 1 4 4"></path>
              <path d="M16 6h.01"></path>
            </svg>
          </div>
        </div>
        <div class="step-content">
          <h3 class="gradient-text">Compartilha</h3>
          <p>
            WhatsApp, Instagram, link na bio, email — onde o seu cliente
            estiver, o link funciona.
          </p>
        </div>
      </div>

      <!-- Connector -->
      <div class="step-connector" aria-hidden="true">
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2.5"
          class="pulse-anim"
        >
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </div>

      <!-- Step 3 -->
      <div class="step-card" style="--delay: 0.9s">
        <div class="step-icon-wrapper">
          <span class="step-number">3</span>
          <div class="step-icon">
            <svg
              width="28"
              height="28"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.75"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <polyline points="20 6 9 17 4 12"></polyline>
            </svg>
          </div>
        </div>
        <div class="step-content">
          <h3>Recebe</h3>
          <p>
            Cliente paga por PIX. O valor cai na sua carteira — em reais ou
            convertido automaticamente para cripto, você decide.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .home-process-section {
    padding: 5rem 1.25rem;
    position: relative;
    z-index: 2;
    text-align: center;
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .home-process-section {
      padding: 8rem 2rem;
    }
  }

  .section-bg-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80vw;
    height: 80vw;
    background: radial-gradient(
      circle at center,
      rgba(255, 0, 122, 0.07) 0%,
      transparent 70%
    );
    z-index: 0;
    pointer-events: none;
  }

  .home-container {
    max-width: 1100px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .home-title {
    font-size: clamp(2rem, 6vw, 3rem);
    font-weight: 800;
    margin-bottom: 1.25rem;
    color: #fff;
    line-height: 1.1;
    letter-spacing: -0.03em;
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .gradient-text {
    background: linear-gradient(90deg, #ff007a 0%, #ff4da6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .home-subtitle {
    font-size: clamp(0.95rem, 2.5vw, 1.15rem);
    color: rgba(255, 255, 255, 0.5);
    margin: 0 auto 3rem;
    max-width: 640px;
    line-height: 1.65;
    animation: fadeInUp 0.8s ease-out 0.35s both;
  }

  @media (min-width: 768px) {
    .home-subtitle {
      margin-bottom: 4rem;
    }
  }

  /* ── Step grid ── */
  .process-steps {
    display: flex;
    justify-content: center;
    align-items: stretch;
    gap: 0;
  }

  /* ── Card ── */
  .step-card {
    flex: 1;
    max-width: 320px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    background: rgba(28, 28, 30, 0.72);
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 24px;
    padding: 2rem 1.5rem 2rem;
    position: relative;
    backdrop-filter: blur(28px);
    -webkit-backdrop-filter: blur(28px);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.06) inset,
      0 20px 40px rgba(0, 0, 0, 0.35);
    transition:
      transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      box-shadow 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      border-color 0.3s ease;
    -webkit-tap-highlight-color: transparent;
    animation: fadeInUp 0.7s ease-out var(--delay, 0.5s) both;
  }

  @media (min-width: 768px) {
    .step-card {
      padding: 2.5rem 1.75rem;
    }
  }

  @media (hover: hover) {
    .step-card:hover {
      transform: translateY(-6px);
      border-color: rgba(255, 255, 255, 0.15);
      box-shadow:
        0 1px 0 rgba(255, 255, 255, 0.08) inset,
        0 28px 56px rgba(0, 0, 0, 0.45);
    }
  }

  .step-card:active {
    transform: scale(0.975);
    transition: transform 0.12s ease;
  }

  /* Highlight (step 2) */
  .step-card.highlight {
    background: rgba(255, 0, 122, 0.08);
    border-color: rgba(255, 0, 122, 0.22);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.06) inset,
      0 20px 40px rgba(0, 0, 0, 0.35),
      0 0 40px rgba(255, 0, 122, 0.06);
  }

  @media (hover: hover) {
    .step-card.highlight:hover {
      border-color: rgba(255, 0, 122, 0.4);
      box-shadow:
        0 1px 0 rgba(255, 255, 255, 0.08) inset,
        0 28px 56px rgba(0, 0, 0, 0.45),
        0 0 50px rgba(255, 0, 122, 0.1);
    }
  }

  /* ── Icon wrapper ── */
  .step-icon-wrapper {
    position: relative;
    margin-bottom: 1.5rem;
    flex-shrink: 0;
  }

  /* Step number — iOS notification badge style */
  .step-number {
    position: absolute;
    top: -7px;
    right: -7px;
    width: 20px;
    height: 20px;
    background: #ff007a;
    color: #fff;
    font-size: 0.7rem;
    font-weight: 800;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow:
      0 0 0 2px rgba(28, 28, 30, 0.9),
      0 2px 8px rgba(255, 0, 122, 0.5);
    z-index: 2;
    letter-spacing: 0;
  }

  /* Icon box — iOS app-icon squircle */
  .step-icon {
    width: 68px;
    height: 68px;
    border-radius: 18px; /* squircle approximation */
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    color: rgba(255, 255, 255, 0.85);
    transition:
      background 0.3s ease,
      border-color 0.3s ease,
      box-shadow 0.3s ease;
  }

  .step-card.highlight .step-icon {
    background: rgba(255, 0, 122, 0.14);
    border-color: rgba(255, 0, 122, 0.25);
    color: #ff4da6;
    box-shadow: 0 0 20px rgba(255, 0, 122, 0.15);
  }

  @media (hover: hover) {
    .step-card:hover .step-icon {
      background: rgba(255, 255, 255, 0.09);
      border-color: rgba(255, 255, 255, 0.18);
    }
    .step-card.highlight:hover .step-icon {
      background: rgba(255, 0, 122, 0.2);
      border-color: rgba(255, 0, 122, 0.4);
    }
  }

  /* ── Text content ── */
  .step-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
  }

  .step-card h3 {
    font-size: 1.2rem;
    font-weight: 700;
    color: #fff;
    letter-spacing: -0.025em;
    line-height: 1.2;
    margin: 0;
  }

  .step-card p {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.52);
    line-height: 1.6;
    margin: 0;
  }

  /* ── Connector ── */
  .step-connector {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 0.25rem;
    margin-top: 2rem; /* align with icon center */
    color: rgba(255, 255, 255, 0.18);
    flex-shrink: 0;
    align-self: flex-start;
    margin-top: calc(2rem + 34px); /* padding + half icon height */
  }

  .pulse-anim {
    animation: pulse-right 1.8s ease-in-out infinite;
  }

  @keyframes pulse-right {
    0%,
    100% {
      transform: translateX(0);
      opacity: 0.18;
    }
    50% {
      transform: translateX(4px);
      opacity: 0.55;
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(24px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ── Mobile: iOS cell layout ── */
  @media (max-width: 767px) {
    .process-steps {
      flex-direction: column;
      align-items: stretch;
      gap: 0;
    }

    /* Grouped table feel — cards share borders */
    .step-card {
      flex-direction: row;
      align-items: flex-start;
      text-align: left;
      max-width: 100%;
      border-radius: 0;
      border-bottom-color: transparent;
      padding: 1.25rem 1.25rem;
      gap: 1rem;
      box-shadow: none;
    }

    /* Round top card */
    .process-steps .step-card:first-child {
      border-radius: 20px 20px 0 0;
    }

    /* Round bottom card */
    .process-steps .step-card:last-child {
      border-radius: 0 0 20px 20px;
      border-bottom-color: rgba(255, 255, 255, 0.09);
    }

    .step-icon-wrapper {
      margin-bottom: 0;
    }

    .step-icon {
      width: 56px;
      height: 56px;
      border-radius: 14px;
    }

    .step-icon svg {
      width: 22px;
      height: 22px;
    }

    .step-number {
      top: -5px;
      right: -5px;
      width: 17px;
      height: 17px;
      font-size: 0.62rem;
    }

    .step-card h3 {
      font-size: 1.05rem;
    }

    .step-card p {
      font-size: 0.855rem;
    }

    /* Hide connectors on mobile */
    .step-connector {
      display: none;
    }

    /* Divider between cells */
    .step-card:not(:last-child) {
      border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    }
  }

  @media (max-width: 479px) {
    .step-card {
      padding: 1.1rem 1rem;
      gap: 0.875rem;
    }

    .step-icon {
      width: 52px;
      height: 52px;
    }
  }
</style>
