---

---

<section id="pricing" class="pricing-section">
  <div class="pricing-container">
    <h2 class="pricing-title">
      Custos simples.<br /><span class="gradient-text">Sem surpresa depois.</span
      >
    </h2>

    <div class="pricing-table">
      <div class="pricing-row">
        <div class="pricing-mode">
          <span class="mode-badge pix">PIX</span>
          <span class="mode-arrow">→</span>
          <span class="mode-badge pix">PIX</span>
        </div>
        <div class="pricing-rate">2,49%</div>
        <div class="pricing-desc">por pagamento recebido</div>
      </div>

      <div class="pricing-divider"></div>

      <div class="pricing-row highlight">
        <div class="pricing-mode">
          <span class="mode-badge pix">PIX</span>
          <span class="mode-arrow">→</span>
          <span class="mode-badge crypto">Cripto</span>
        </div>
        <div class="pricing-rate">
          0,5%
          <span class="pricing-badge">diferencial</span>
        </div>
        <div class="pricing-desc">sobre o valor convertido</div>
      </div>
    </div>

    <p class="pricing-note">
      Você só paga quando recebe. Sem mensalidade. Sem saldo preso.
    </p>
  </div>
</section>

<style>
  .pricing-section {
    padding: 5rem 1.25rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .pricing-section {
      padding: 7rem 2rem;
    }
  }

  .pricing-container {
    max-width: 640px;
    margin: 0 auto;
    text-align: center;
  }

  .pricing-title {
    font-size: clamp(1.8rem, 5vw, 2.75rem);
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.03em;
    line-height: 1.15;
    margin-bottom: 3rem;
  }

  .gradient-text {
    background: linear-gradient(90deg, #ff007a 0%, #ff4da6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .pricing-table {
    background: rgba(15, 15, 15, 0.5);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    overflow: hidden;
    margin-bottom: 1.5rem;
  }

  .pricing-row {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    padding: 1.5rem 1.75rem;
    gap: 1rem;
    transition: background 0.2s ease;
  }

  @media (min-width: 480px) {
    .pricing-row {
      padding: 1.75rem 2.5rem;
    }
  }

  .pricing-row.highlight {
    background: rgba(255, 0, 122, 0.04);
  }

  .pricing-divider {
    height: 1px;
    background: rgba(255, 255, 255, 0.06);
    margin: 0 1.75rem;
  }

  @media (min-width: 480px) {
    .pricing-divider {
      margin: 0 2.5rem;
    }
  }

  .pricing-mode {
    display: flex;
    align-items: center;
    gap: 8px;
    justify-content: flex-start;
  }

  .mode-badge {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 4px 10px;
    border-radius: 6px;
  }

  .mode-badge.pix {
    background: rgba(0, 200, 100, 0.12);
    color: #00c864;
    border: 1px solid rgba(0, 200, 100, 0.2);
  }

  .mode-badge.crypto {
    background: rgba(255, 0, 122, 0.12);
    color: #ff4da6;
    border: 1px solid rgba(255, 0, 122, 0.2);
  }

  .mode-arrow {
    color: rgba(255, 255, 255, 0.3);
    font-size: 0.9rem;
    font-weight: 300;
  }

  .pricing-rate {
    font-size: clamp(1.4rem, 4vw, 1.75rem);
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.02em;
    display: flex;
    align-items: center;
    gap: 8px;
    justify-content: center;
  }

  .pricing-badge {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 5px;
    background: rgba(255, 0, 122, 0.15);
    color: #ff4da6;
    border: 1px solid rgba(255, 0, 122, 0.25);
    vertical-align: middle;
  }

  .pricing-desc {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.35);
    font-weight: 500;
    text-align: right;
  }

  .pricing-note {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.4);
    line-height: 1.5;
  }

  @media (max-width: 479px) {
    .pricing-section {
      padding: 4rem 1rem;
    }

    .pricing-row {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 0.4rem;
      padding: 1.25rem 1.25rem;
      min-height: 44px;
    }

    .pricing-mode {
      justify-content: center;
    }

    .pricing-rate {
      font-size: 1.5rem;
      justify-content: center;
    }

    .pricing-desc {
      text-align: center;
    }

    .pricing-badge {
      display: none;
    }
  }
</style>
