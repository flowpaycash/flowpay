---
import { Image } from 'astro:assets';
import flowpayLogo from '../../public/img/flowpay-logo.png';
---

<section class="hero-premium">
  <div class="hero-bg-glow"></div>
  <div class="hero-content">
    <div class="brand-reveal">
      <Image
        src={flowpayLogo}
        alt="FlowPay Logo"
        class="premium-logo"
        height={200}
        loading="eager"
      />
    </div>

    <h1 class="premium-title">
      A experiência de <span class="gradient-text">pagamento</span><br />
      que você <span class="gradient-text">merece</span>.
    </h1>

    <p class="premium-subtitle">
      Tecnologia avançada não precisa ser complexa. O FlowPay une o poder de uma
      infraestrutura robusta com um design deslumbrante, criado para quem exige
      excelência.
    </p>

    <div class="hero-glass-cta">
      <div class="cta-actions">
        <a href="/registro" class="btn-premium primary">
          Criar conta grátis
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            ><line x1="5" y1="12" x2="19" y2="12"></line><polyline
              points="12 5 19 12 12 19"></polyline></svg
          >
        </a>
        <a href="#features" class="btn-premium secondary">
          Entender a Tecnologia
        </a>
      </div>

      <div class="cta-stats">
        <div class="stat">
          <span class="stat-value">Real-Time</span>
          <span class="stat-label">Velocidade</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-value">100%</span>
          <span class="stat-label">Fluidez</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-premium {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding: 80px 20px;
    background: transparent;
  }

  .hero-bg-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100vw;
    height: 100vw;
    background: radial-gradient(
      circle at center,
      rgba(255, 0, 122, 0.15) 0%,
      transparent 70%
    );
    z-index: 0;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 1200px;
  }

  .brand-reveal {
    margin-bottom: 60px;
    animation:
      floatingLogo 6s ease-in-out infinite,
      fadeInDown 1.2s cubic-bezier(0.2, 0.8, 0.2, 1);
    position: relative;
    display: inline-block;
  }

  /* Cybernetic glow behind the logo */
  .brand-reveal::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 140%;
    height: 140%;
    background: radial-gradient(
      circle,
      rgba(255, 0, 122, 0.2) 0%,
      transparent 70%
    );
    filter: blur(30px);
    z-index: -1;
    animation: logoGlowPulse 4s ease-in-out infinite alternate;
  }

  .premium-logo {
    height: 200px;
    width: auto;
    filter: drop-shadow(0 0 15px rgba(255, 0, 122, 0.6))
      drop-shadow(0 0 30px rgba(0, 212, 255, 0.3));
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .premium-logo:hover {
    transform: scale(1.1) rotate(2deg);
    filter: drop-shadow(0 0 25px rgba(255, 0, 122, 0.8))
      drop-shadow(0 0 50px rgba(0, 212, 255, 0.5));
  }

  @keyframes floatingLogo {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-15px);
    }
  }

  @keyframes logoGlowPulse {
    0% {
      opacity: 0.5;
      transform: translate(-50%, -50%) scale(1);
    }
    100% {
      opacity: 1;
      transform: translate(-50%, -50%) scale(1.2);
    }
  }

  .premium-title {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #ffffff;
    margin-bottom: 24px;
    animation: fadeInUp 0.8s ease-out 0.2s both;
    text-shadow: 0 0 40px rgba(0, 0, 0, 0.5);
  }

  .gradient-text {
    background: linear-gradient(90deg, #ff007a 0%, #ff7a00 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .premium-subtitle {
    font-size: clamp(1.1rem, 3vw, 1.4rem);
    color: rgba(255, 255, 255, 0.7);
    max-width: 650px;
    margin: 0 auto 48px;
    line-height: 1.6;
    animation: fadeInUp 0.8s ease-out 0.4s both;
  }

  .hero-glass-cta {
    background: rgba(15, 15, 15, 0.4);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 32px;
    padding: 40px;
    box-shadow:
      0 25px 50px rgba(0, 0, 0, 0.4),
      inset 0 1px 1px rgba(255, 255, 255, 0.1);
    display: flex;
    flex-direction: column;
    gap: 32px;
    animation: fadeInUp 0.8s ease-out 0.6s both;
  }

  .cta-actions {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-premium {
    padding: 20px 40px;
    border-radius: 20px;
    font-size: 1.1rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: flex;
    align-items: center;
    gap: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .btn-premium.primary {
    background: linear-gradient(135deg, #ff007a, #ff7a00);
    color: white;
    box-shadow: 0 0 30px rgba(255, 0, 122, 0.3);
  }

  .btn-premium.primary:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 0 50px rgba(255, 0, 122, 0.5);
  }

  .btn-premium.secondary {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: white;
  }

  .btn-premium.secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.2);
    transform: translateY(-4px);
  }

  .cta-stats {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 50px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 32px;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-value {
    font-size: 1.8rem;
    font-weight: 800;
    color: #ff007a;
    text-shadow: 0 0 20px rgba(255, 0, 122, 0.4);
  }

  .stat-label {
    font-size: 0.8rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.5);
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: rgba(255, 255, 255, 0.1);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .hero-glass-cta {
      padding: 30px 20px;
      gap: 25px;
    }
    .btn-premium {
      width: 100%;
      justify-content: center;
    }
    .cta-stats {
      gap: 30px;
    }
    .stat-value {
      font-size: 1.5rem;
    }
  }
</style>
