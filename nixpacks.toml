# FlowPay — Nixpacks build config
# Only declares extra system libs needed for better-sqlite3 (C++ native addon).
# Everything else (Node 20, pnpm via corepack, install, build, start)
# is auto-detected from package.json + pnpm-lock.yaml.

[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake"]
